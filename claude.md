Repère dans le workspace le dossier `Fred`, puis lis `workspace-organization.md`.

Suis ensuite l'ordre de lecture et les conventions qu'il décrit.

En particulier :

- lis les conventions transverses référencées dans `Fred` ;
- identifie le projet principal concerné par la demande ;
- lis `_workspace/atlas.md` et `_workspace/TODO.md` de ce projet avant tout travail substantiel ;
- traite `_workspace/memories/` comme mémoire partagée inter-agents ;
- mets à jour cette mémoire quand un handoff ou un fait stable utile apparaît.

Si le dossier `Fred` n'est pas présent dans le workspace courant, utilise les fichiers locaux du repo comme fallback et explicite que le bootstrap global n'est pas disponible.